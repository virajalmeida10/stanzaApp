spring.application.name=stanzaApp

#DB Configuration:
spring.datasource.url=${DB_URL:jdbc:postgresql://localhost:5432/StranzaAppDB}
spring.datasource.username=${DB_USER:virajalmeida}
spring.datasource.password=${DB_PASSWORD:}
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

stripe.secret.key=${STRIPE_SECRET_KEY:sk_test_placeholder}
stripe.webhook.secret=${STRIPE_WEBHOOK_SECRET:whsec_placeholder}

# Render gives the app its port through the PORT envelope; 8080 is the local fallback
server.port=${PORT:8080}
server.servlet.context-path=/api/v1

jwt.secretKey=${JWT_SECRET:dfgyt655r6tygyu7y768uy86g646568ubte4w32456h7j89kminhyjuinu875}
frontend.url=${FRONTEND_URL:http://localhost:3000}

# Email (SMTP) — reads the MAIL_* env vars you set on Render
spring.mail.host=${MAIL_HOST:smtp.gmail.com}
spring.mail.port=${MAIL_PORT:587}
spring.mail.username=${MAIL_USERNAME:}
spring.mail.password=${MAIL_PASSWORD:}
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true

mail.from=${MAIL_FROM:${spring.mail.username}}

anthropic.api.key=${ANTHROPIC_API_KEY:}
